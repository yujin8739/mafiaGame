<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="chatMapper">

    <select id="selectAllRooms" resultType="GameRoom">
        SELECT * FROM GAME_ROOM
    </select>

    <select id="selectRoomById" parameterType="int" resultType="GameRoom">
        SELECT * FROM GAME_ROOM WHERE ROONNO = #{roonNo}
    </select>

    <insert id="insertMessage" parameterType="Message">
        INSERT INTO MESSAGE (ROONNO, MSGNO, TYPE, MSG, USERNAME, CHATDATE)
        VALUES (#{roonNo}, #{msgNo}, #{type}, #{msg}, #{userName}, #{chatDate})
    </insert>

    <select id="selectMessagesByRoom" parameterType="int" resultType="Message">
        SELECT * FROM MESSAGE WHERE ROONNO = #{roonNo} ORDER BY CHATDATE ASC
    </select>

    <insert id="insertRoom" parameterType="GameRoom">
        INSERT INTO GAME_ROOM (ROONNO, ROOMNAME, USERLIST, MAX, MIN, PASSWORD, STATUS)
        VALUES (#{roonNo}, #{roomName}, #{userList}, #{max}, #{min}, #{password}, #{status})
    </insert>

</mapper>